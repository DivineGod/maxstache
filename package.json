{
  "name": "maxstache",
  "version": "1.0.5",
  "description": "Minimalist mustache template replacement",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "deps:pkg": "ncu",
    "deps:update": "ncu -a",
    "test": "npm run deps && NODE_ENV=test babel-node test",
    "test:cov": "npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/maxstache",
  "keywords": [
    "mustache",
    "template",
    "minstache"
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel": "^5.8.23",
    "dependency-check": "^2.5.1",
    "istanbul": "^0.3.22",
    "tape": "^4.2.1"
  },
  "files": [
    "index.js",
    "bin/*"
  ]
}
